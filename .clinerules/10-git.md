# Git

- Do not rewrite Git history.
- Do not reset or discard user changes.
- Never use destructive Git commands unless explicitly requested.
- Verify Git history and state with Git commands (e.g. `git log`, `git status`, `git diff`) before citing or asserting them, rather than relying on memory.
